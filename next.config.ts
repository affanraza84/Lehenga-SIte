import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  theme: {
  extend: {
    colors: {
      brandBrown: "#421C05",
      brandOrange: "#E5973D",
      brandText: "#2C1810",
    },
  },
},

};

export default nextConfig;
